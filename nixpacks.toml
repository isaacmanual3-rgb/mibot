[phases.setup]
nixPkgs = ["python311", "pkg-config", "libmysqlclient-dev", "libsodium"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
